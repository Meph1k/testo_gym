<div class="authform">
  <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :role => 'form'}) do |f| %>
    <h3>Logowanie</h3>
    <%= devise_error_messages! %>
    <div class="form-group">
      <%- if devise_mapping.registerable? %>
        <%= link_to 'Rejestracja', new_registration_path(resource_name), class: 'right'  %>
      <% end -%>
      <%= f.label :email, "Email" %>
      <%= f.email_field :email, :autofocus => true, class: 'form-control' %>
    </div>
    <div class="form-group">
      <%- if devise_mapping.recoverable? %>
        <%= link_to "Zapomniałeś hasła?", new_password_path(resource_name), class: 'right' %>
      <% end -%>
      <%= f.label :password, "Hasło" %>
      <%= f.password_field :password, class: 'form-control' %>
    </div>
      <%= f.submit 'Zaloguj się', class: 'button right' %>
      <% if devise_mapping.rememberable? -%>
        <div class="checkbox" style="width:150px">
          <label>
            <%= f.check_box :remember_me %> Zapamiętaj mnie
          </label>
        </div>
      <% end -%>
  <% end %>
</div>
